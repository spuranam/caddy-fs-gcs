// Package main is the entry point for the Caddy server with the GCS filesystem
// plugin. It imports the standard Caddy modules and the GCS plugin, then
// delegates to Caddy's main command handler.
package main
